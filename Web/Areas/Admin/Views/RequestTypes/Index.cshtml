@model IEnumerable<ccsc.DataLayer.Entities.Requests.RequestType>

@{
    ViewData["Title"] = "فهرست";
    
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReplyByEnum)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReplyByEnum)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RequestTypeId">ویرایش</a> |
                <a asp-action="Details" asp-route-id="@item.RequestTypeId">جزئیات</a> |
                <a asp-action="Delete" asp-route-id="@item.RequestTypeId">حذف</a>
            </td>
        </tr>
}
    </tbody>
</table>
