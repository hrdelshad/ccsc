@model IEnumerable<ccsc.DataLayer.Entities.Services.ServiceStatus>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsOk)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsOk)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ServiceStatusId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ServiceStatusId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ServiceStatusId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
