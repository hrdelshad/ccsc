@model IEnumerable<ccsc.DataLayer.Entities.Requests.RequestStatus>

@{
    ViewData["Title"] = "فهرست";
    
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsActive)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsActive)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RequestStatusId">ویرایش</a> |
                <a asp-action="Details" asp-route-id="@item.RequestStatusId">جزئیات</a> |
                <a asp-action="Delete" asp-route-id="@item.RequestStatusId">حذف</a>
            </td>
        </tr>
}
    </tbody>
</table>
