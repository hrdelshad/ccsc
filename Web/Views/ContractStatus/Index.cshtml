@model IEnumerable<ccsc.DataLayer.Entities.Contracts.ContractStatus>

@{
    ViewData["Title"] = "فهرست";
    
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsOk)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsOk)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContractStatusId">ویرایش</a> |
                <a asp-action="Details" asp-route-id="@item.ContractStatusId">جزئیات</a> |
                <a asp-action="Delete" asp-route-id="@item.ContractStatusId">حذف</a>
            </td>
        </tr>
}
    </tbody>
</table>
