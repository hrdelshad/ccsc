@model IEnumerable<ccsc.DataLayer.Entities.Customers.SqlVersion>

@{
    ViewData["Title"] = "فهرست";
    
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SqlVersionId">ویرایش</a> |
                <a asp-action="Details" asp-route-id="@item.SqlVersionId">جزئیات</a> |
                <a asp-action="Delete" asp-route-id="@item.SqlVersionId">حذف</a>
            </td>
        </tr>
}
    </tbody>
</table>
