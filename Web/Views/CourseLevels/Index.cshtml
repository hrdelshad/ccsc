@model IEnumerable<ccsc.DataLayer.Entities.Courses.CourseLevel>

@{
    ViewData["Title"] = "فهرست";
    
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourseLevelId">ویرایش</a> |
                <a asp-action="Details" asp-route-id="@item.CourseLevelId">جزئیات</a> |
                <a asp-action="Delete" asp-route-id="@item.CourseLevelId">حذف</a>
            </td>
        </tr>
}
    </tbody>
</table>
